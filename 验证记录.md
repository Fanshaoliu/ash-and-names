# v3 验证记录

验证日期：2026-09-25。游戏改名《明天照常营业》。旧版战斗与成长代码已移除，本记录只针对新版探索流程。

## 自动验证

`node tests/engine-test.cjs`：通过 952 个断言。

- 35 张地图的初始落脚点、全部 NPC / 线索 / 闲事的相邻交互位置、所有门的入口和返回落脚点均可达。
- 8 案 × 2 调查路线 × 3 处置，共 48 个章节内组合；按六条完整的八案测试路线驱动实际剧情逻辑。
- 每案恰需三条线索；缺线索不能作最终处置。完成案件后推进下一案。
- 门按章节与路线开放，旧校准室必须先看时钟，再选 17:10；错误可重试。
- 四帧行走均实际推进，路径结束恢复站立姿势；物体的落脚格不能穿过。
- 存档往返校验、v2 存档拒绝、不合法位置与未来线索拒绝。
- 每案两个回溯点；回溯撤回后续线索和决定，保留已见结局收藏。
- 结局条件与八件闲事、32 组概念的结构检查。

额外检查：48 个主角团源帧各不相同；四个人、每人三组方向各四帧，向右使用左向镜像。新图片尺寸、透明通道与脚底元数据已检查。HTML 的本地资源链接无缺失，JS 语法检查通过。

这些测试使用模拟 DOM 驱动真实游戏逻辑，不是完整浏览器或每一种跨章组合的穷举。交叉条件另作定向断言。

## 真实浏览器

使用 Codex 内置浏览器检查桌面与手机竖屏布局。完成新游戏开场、城镇进入早餐铺、NPC 交谈、调查路线选择、两条共同线索、早餐铺退出返回原门口、进入修理所、读取线索、分歧页回溯，回溯后线索清空并恢复原章节。

通过界面的文件选择器导入八案完成测试存档，恢复大地图；实际从大地图的城堡入口进入昼务城，再进入临时议事厅。检查终章可选方案与条件提示，实际观看“尚未完成的星期二”和全收集洗碗尾声，确认结局记录后仍可继续探索。

人物头像可辨，主角团存在分方向步行。场景采用统一逻辑像素、脚底阴影与纵向遮挡；家具按店铺、办公室、档案馆等用途布置。桌面画布的宽度留白问题已修复。手机界面保留触屏方向键、A 键和滚动对话框。

没有用真实 iPhone / Android 硬件执行测试；竖屏验证来自浏览器视口，不等同于全部移动浏览器兼容认证。声音为可关闭的简单合成音，移动浏览器需点按钮启用。

## 存档与发布

v3 使用独立存档键，旧版浏览器进度保留。剧情和地图改变后不自动迁移旧版进度。跨设备通过 JSON 导出 / 导入；没有云同步。

公开版本由 GitHub Pages 从 `main` 部署。代码内不包含账号凭据，不依赖在线大模型或运行密钥。发布后检查 Pages 构建提交、静态文件与公开页面。
